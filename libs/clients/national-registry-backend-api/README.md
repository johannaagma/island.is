# clients-national-registry-backend-api

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test clients-national-registry-backend-api` to execute the unit tests via [Jest](https://jestjs.io).
